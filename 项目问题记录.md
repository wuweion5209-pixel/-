# 项目问题记录

## 2026-04-05

### 问题 1：fusion.py 单例模式丢失
- **现象**：`ImportError: cannot import name 'get_fusion_retriever'`
- **原因**：提交时文件被覆盖回旧版本，类+单例模式代码丢失
- **解决**：重新写入 `FusionRetriever` 类和 `get_fusion_retriever()` 函数

### 问题 2：变量名拼写错误
- **现象**：`'BM25Retriever' object has no attribute 'metadatas'`
- **原因**：`bm25_retriever.py` 中 `self.metadatas` 应为 `self._metadatas`
- **解决**：修正变量名

### 问题 3：重排序模型无法下载
- **现象**：`bge-reranker-base is not a valid model identifier`
- **原因**：国内无法访问 HuggingFace 官方源
- **解决**：
  - 设置镜像源：`os.environ["HF_ENDPOINT"] = "https://hf-mirror.com"`
  - 改用镜像地址：`model_name = f"hf-mirror/{model_name}"`
  - 禁用重排序（配置中 `rerank_enabled: False`）

### 问题 4：检索结果太少
- **现象**：上传的论文有几十个文档，但只检索到 5 条
- **原因**：
  - `vector_k` 和 `bm25_k` 默认值 10 太小
  - `similarity_threshold` 默认值 0.4 太高
- **解决**：
  - `vector_k` 和 `bm25_k` 改为 20
  - `similarity_threshold` 改为 0.3

### 问题 5：查询扩展
- **现象**：检索"参考文献"相关内容时搜不到
- **原因**：关键词不匹配
- **解决**：添加查询扩展功能，当检索结果 < 5 时自动追加关键词（参考文献、引用、references 等）

### 问题 6：字数/详细程度问题（误报）
- **现象**：LLM 回答不符合用户要求的字数或详细程度
- **原因**：测试方法问题 —— 在同一个对话里测试，历史记录影响了回答
- **解决**：每次测试创建新会话

---

## 配置优化总结

| 参数 | 原值 | 新值 | 作用 |
|------|------|------|------|
| vector_k | 10 | 20 | 增加向量检索返回数量 |
| bm25_k | 10 | 20 | 增加 BM25 检索返回数量 |
| similarity_threshold | 0.4 | 0.3 | 降低阈值，避免漏掉相关文档 |
| rerank_enabled | True | False | 禁用（模型加载失败） |
| query_expansion | False | True | 启用查询扩展 |

---

## 待优化项（后续）

1. ~~Chunk Size 优化~~：PDF 存入向量库时按段落分块，而非整页存入 ✅ 已完成
2. **重排序模型本地化**：下载模型到本地，避免每次加载失败
3. **Tool 扩展**：增加计算器、天气、搜索等工具
4. **重排序启用**：解决模型下载问题后重新开启

---

## Chunk Size 优化（2026-04-05）

### 背景
- 原来 PDF 存入向量库时按**整页**存储，导致：
  - 一页 PDF 很长，包含多个主题
  - 检索时返回整页，可能包含用户不需要的内容

### 实现
- 新增 `app/services/rag/chunker.py` - TextChunker 类
- 逻辑：
  1. 先按**段落**分割（双换行符 \n\n）
  2. 按**字符数**限制分块（默认 500 字符）
  3. 相邻块之间**重叠** 50 字符，保持上下文连续性
- 配置参数（在 config.py 中）：
  ```python
  chunk_size: int = 500       # 每块最大字符数
  chunk_overlap: int = 50     # 块之间重叠字符数
  min_chunk_size: int = 100   # 最小块字符数
  ```

### 效果
- 原来：1 页 PDF → 1 个向量
- 现在：1 页 PDF → 多个块（每块 ~500 字符）
- 检索更精准，返回的内容更聚焦